Archive Mode 전환

show parameter archive

log_archive_format                   string      %t_%s_%r.dbf	-> t - thread#, s - log_sequence#, r - resetlogs id
log_archive_dest_1~31				LOG_ARCHIVE_DEST_1='LOCATION=/u01/archive'          -- 로컬경로 or USE_DB_RECOVERY_FILE_DEST
									LOG_ARCHIVE_DEST_2='SERVICE=standby1 LGWR ASYNC'    -- Data Guard 원격, 원격 standby tns alias	
									
									Mandatory / Optional -> 실패시 DB 중단 여부		
									Sync / Async -> Dataguard 동기화 방식
									LGWR / ARCH  -> Data guard 전송 프로세스 선택
									..
									..
									
log_archive_dest_state_1~31			Enable -> 기본값, 활성
									Defer  -> 임시 비활성(설정은 유지)
									alternate -> 다른 dest 실패시 대체 활성화
									
log_archive_config					rac/data guard 환경에서 db_unique_name 목록 정의  / 
log_archive_max_processes           integer     4(default) arch 백그라운드 프로세스 최대 갯수
log_archive_min_succeed_dest        integer     1(default) 아카이빙 성공으로 인정할 최소 목적지 수
log_archive_start                    boolean     FALSE	/ 이전버전 레거시
log_archive_trace                    integer     0		/ 아카이브 프로세스 디버그 레벨
log_archive_dest					-> 이전버전 레거시 


파라미터 값 수정후 DB 종료후

startup mounnt
alter database archivelog;
alter database open;

SYS@ORCL > archive log list
Database log mode              Archive Mode
Automatic archival             Enabled
Archive destination            /home/oracle/orcl_arc/
Oldest online log sequence     14
Next log sequence to archive   16
Current log sequence           16


Archive Mode 해제
DB 종료후

startup mount
alter database noarchivelog;
alter database open;

